apiVersion: k1s.io/v1
kind: EdgeIngressPolicy
metadata:
  name: ingress-matrix-sticky-policy
  namespace: default
spec:
  loadBalancing:
    strategy: round_robin
  stickiness:
    mode: cookie
    cookie:
      name: k1s_route
      ttlSeconds: 3600

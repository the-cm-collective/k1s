https://blue.home.arpa {
    log {
        output stdout
        format console
    }
    # Ensure upstream HSTS does not stick during dev
    header -Strict-Transport-Security
    reverse_proxy host.docker.internal:32784
}

apiVersion: ae.dev/v1alpha1
kind: Deployment
metadata:
  name: bad-secret
spec:
  image: alpine:3.20
  secretRefs:
    - name: missing-secret
      path: /tmp/k1s-maintenance-missing-secret.yaml
      env:
        - name: SECRET_VALUE
          key: SECRET_VALUE
